# 财经资讯聚合平台 —— 需求规格说明书（完善版）

> 项目名称：财经资讯（A股投资者资讯聚合平台）
> 文档版本：v1.1（由初版需求表完善而来）
> 编制视角：财经专家 / 软件架构师 / 产品经理 三方交叉评审
> 修订记录：v0.1 原始需求 → v1.0 结构化补齐（用户/场景、需求编号与优先级、验收标准、数据源合规、处理算法、架构、非功能指标、里程碑、风险）。
> v1.1：按业务方评审结论**引入延时行情联动**（新增 DR-12 / DP-7 / FR-17，同步更新边界、KPI、架构、数据模型、API、里程碑、风险），其余开放问题按默认假设锁定（见 §12）。

---

## 0. 阅读指引

- **需求编号规则**：
  - `DR` = 数据源需求（Data Source）
  - `DP` = 数据处理需求（Data Processing）
  - `FR` = 功能需求（Function）
  - `NFR` = 非功能需求（Non-Functional）
  - `COM` = 合规需求（Compliance）
  - `R` = 数据保留/存储需求（Retention）
- **优先级定义（MoSCoW 法）**：
  - **P0**：MVP 必须具备，缺则产品无法成立（首期交付）。
  - **P1**：应当具备，决定产品价值深度（二期交付）。
  - **P2**：可以有，增强体验或为商业化探索（三期/可选）。
- **总体假设**（见 §12 评审结论）：
  - 初期定位为**面向 A 股个人投资者/团队的资讯工具**，推送默认走企业微信；
  - **不含**完整 K线/Level-2/交易/荐股类功能（红线，见 §6）；已确认引入**延时行情快照联动**（仅用于异动加权与标注，见 DR-12/DP-7/FR-17）；
  - 正文数据在线保留 **7 天**（见 §7）。

---

## 1. 项目定位与目标（产品经理视角）

### 1.1 一句话定位
面向 **A股个人投资者** 的金融资讯聚合网站，实时汇聚国内外财经新闻、市场快讯、官方公告及社交媒体传闻，通过分类、去重、关联个股与重要消息甄别，降低散户的"信息噪音"与"信息时滞"。

### 1.2 目标用户与画像
| 用户角色 | 画像 | 核心诉求 | 典型使用时段 |
|---|---|---|---|
| 短线/盯盘散户 | 每日盘中多次查看，持仓集中 | 快讯时效、重要消息第一时间触达 | 交易时段（9:15–11:30 / 13:00–15:00） |
| 中长线价值投资者 | 关注公告、基本面 | 公告权威、防"小作文"误导 | 盘后 15:00–21:00（公告披露高峰） |
| 研究员/投顾助理（内部） | 舆情监控、消息面复盘 | 多源去重、热度排名、可追溯原文 | 全天，含盘前晨会 |
| 运营/管理员 | 维护数据源与推送 | 后台可配置、可审计 | 非交易时段 |

### 1.3 核心价值主张
1. **快**：快讯端到端延迟 ≤ 3 分钟；重要消息主动推送。
2. **准**：官方公告与传闻严格区分；去重防刷屏；标注"未经证实"。
3. **全**：快讯/深度/公告/传闻/宏观 一站聚合；自动关联到个股。
4. **省心**：自选股订阅，只推与自己相关的"重要消息"。

### 1.4 产品边界（本期明确不做）
- ❌ 不提供完整 K线/技术分析/Level-2 分笔与交易下单；**仅引入"延时行情快照"用于异动联动**（热度与重要消息加权、个股页异动标注，见 DR-12/DP-7/FR-17），不承载任何交易决策，也不构成投资建议。
- ❌ 不提供荐股、买卖点建议、收益承诺（涉及证券投资咨询资质，见 §6）。
- ❌ 不做 UGC 社区（评论/发帖）——评论会引入内容审核成本（P2 再评估）。
- ❌ 不做新闻全文的二次创作/深度解读署名发布（版权风险）。
- ❌ 不做个人理财/账户体系之外的金融交易功能。

### 1.5 成功指标（KPI 草案，需与业务对齐）
| 指标 | 目标（建议） |
|---|---|
| 快讯端到端时效达标率（≤3min） | ≥ 95%（P95） |
| 重要消息用户正向反馈率（点开/有用） | ≥ 60% |
| 企微推送送达率 / 点击率 | ≥ 99% / ≥ 15% |
| 分类准确率（抽检） | ≥ 90%（类型维度） |
| 去重误杀率（把不同新闻判为重复） | ≤ 5% |
| 关联个股准确率（抽检 Top 关联） | ≥ 85% |
| 异动标签命中率（涨停/跌停/龙虎榜名单对照） | ≥ 95% |
| 用户 7 日留存（若有用户体系） | ≥ 25% |
| 平台可用性（服务侧） | ≥ 99.5% |

---

## 2. 用户与核心场景（用例）

| 编号 | 场景 | 用户 | 流程描述 | 涉及需求 |
|---|---|---|---|---|
| UC-01 | 盘中盯快讯 | 短线散户 | 打开首页实时流 → 按"快讯"筛选 → 点击关联个股查看 → 关注个股订阅 | FR-01/02/04/05 |
| UC-02 | 重要消息触达 | 全体 | 系统判定某政策/个股重大消息 → 推送企微 → 点击跳详情 | DP-5/FR-06/09 |
| UC-03 | 盘后看公告 | 价值投资者 | 进入"公告"频道按类型筛选（业绩/重组/减持）→ 查看结构化要点 → 跳巨潮原文 | FR-02/11 |
| UC-04 | 传闻求证 | 全体 | 看到"未经证实"传闻 → 点击查看来源平台与原文链接 → 对照官方公告/澄清 | FR-08/10 |
| UC-05 | 个股舆情复盘 | 研究员 | 搜索个股 → 个股主页聚合 7 日内公告/新闻/传闻 + 情感倾向汇总 | FR-03/04/14 |
| UC-06 | 订阅推送管理 | 已注册用户 | 维护自选股 → 配置推送时段/阈值 → 接收个性化重要消息 | FR-05/09 |
| UC-07 | 后台运维 | 运营/管理员 | 启停数据源、看采集监控、维护词典/黑名单、清理过期数据、查推送日志 | FR-12 |

---

## 3. 数据源需求（DR）

### 3.1 数据源总表
| 编号 | 类别 | 来源 | 内容范围 | 接入方式建议 | 建议采集频率 | 版权/合规要点 | 优先级 | 主要风险 |
|---|---|---|---|---|---|---|---|---|
| DR-01 | 国内快讯 | 财联社（电报） | 实时电报快讯，价值最高 | **无官方开放 API**；仅可网页/App 抓取或与官方洽谈订阅/合作。正式商用前必须确认授权 | 实时，峰值可达数条/分钟 | 电报内容即产品，版权敏感；抓取前需评估 ToS | P0 | 被封/版权投诉/无稳定接口 |
| DR-02 | 国内快讯 | 华尔街见闻 | 快讯+深度，偏宏观/海外 | 网页/RSS 抓取，控制频率 | 分钟级 | 自有版权内容，需谨慎 | P0 | 同上 |
| DR-03 | 国内快讯 | 东方财富 | 资讯中心、个股新闻、市场要闻 | 有公开数据接口（push2 系列 JSON），实现 adapter | 分钟级轮询 | 非官方接口，控制频率与 UA | P0 | 接口变更/频率限制 |
| DR-04 | 国内快讯 | 同花顺 | 快讯/要闻/个股资讯 | 同 DR-03，独立 adapter | 分钟级 | 同上 | P1 | 同上 |
| DR-05 | 官方公告 | 巨潮资讯网 | A股上市公司公告（**法定披露**） | 官方公告检索/页面与文本；建议尽量走其官方数据或规范检索 | 分钟级轮询增量（盘后为高峰） | 证监会指定披露平台；转载须注明出处并链回原文，不得改篡 | P0 | 接口频控 |
| DR-06 | 官方公告 | 上交所 / 深交所（+北交所） | 官网公告/监管函件/问询 | 官网公告页增量抓取，补充 DR-05 缺口 | 分钟级 | 同上；监管文件属公开信息但转载需规范 | P1 | 页面结构变化 |
| DR-07 | 国际新闻 | Finnhub News API | Reuters/Benzinga/GlobeNewswire 等聚合 | 官方 API（免费层有配额，商业用途需付费层级） | 分钟级轮询 | **注意其服务条款对内容再分发/转售的限制**，商用需确认 License | P1 | API 费用/条款限制 |
| DR-08 | 社交媒体 | 雪球热帖 | 热帖标题/摘要/链接（**避免抓全文**） | 雪球无官方开放 API 且反爬严格；建议仅取公开热帖榜单 + 链接，或走第三方舆情数据商 | 5–10 分钟 | 帖子版权归作者/雪球；只收录标题摘要并外链 | P1 | 反爬/版权 |
| DR-09 | 社交媒体 | 微博 / 小红书 | 市场传闻、舆情话题 | **强烈建议使用开放平台授权接口或第三方舆情数据服务**；禁止高频暴力抓取 | 按需（非实时） | 平台 ToS 严、反爬强；文本内容版权与个人信息风险 | P2 | 封号/法律风险最高 |
| DR-10 | 宏观数据（新增建议） | 央行/统计局/证监会等官方渠道、财新/金十（备选） | 货币政策、CPI/PMI、监管政策 | 官方站点/权威媒体 | 事件驱动 | 官方数据可引用，注明来源 | P1 | — |
| DR-11 | 补充源（新增建议） | 交易所投资者互动平台（互动易/上证e互动） | 公司互动问答（有舆情价值） | 官方接口/页面 | 日频 | 公开信息 | P2 | — |
| DR-12 | 行情快照（新增，异动联动） | A股延时行情：最新价/涨跌幅/换手率/成交额（Level-1 延时或日线级，**非 Level-2 分笔**） | 建议：腾讯/新浪公开行情接口 或 Tushare/AkShare，或采购 Level-1 授权行情 | 交易时段 15–30s 轮询快照，非交易时段冻结 | 行情数据有版权与使用条款；免费源限频，需降级策略 | P1 | 免费接口限额/断连/条款收紧 |

> **架构师提示**：每个来源必须封装为**独立可插拔 Adapter**（配置化启停、频率、UA、代理、重试），源不可用时降级不影响全局。

### 3.2 接入策略与频率红线
- 遵守目标站点 `robots.txt`，设置合理 UA 与限速（如 ≥ 2 秒/请求，公告源 5 秒以上）。
- 单源失败重试 ≤ 3 次（指数退避），连续失败触发告警并自动暂停该源。
- 全部走 HTTPS；外链跳转统一走服务端白名单校验。
- 社交媒体类源只收录"标题/摘要/作者/时间/外链"元数据，不镜像正文。

---

## 4. 数据处理需求（DP）（财经专家 + 架构师）

### 4.0 处理流水线总览
```
采集(Adapter) → 校验/标准化 → 去重 → 解析(正文/时间/来源) → 分类 → 情感 → 关联个股 → 热度/重要性 → 入库+索引 → 触发推送
  各环节均打点（耗时/失败率），监控见 NFR-7。
```

### DP-0 字段标准化（架构师）
- 统一时区 **Asia/Shanghai**；同时保留 `published_at`（源发布时间）与 `crawled_at`（采集时间），时效指标以源发布时间为基准。
- 统一格式：正文清洗去广告/去噪，标题去重空格与表情；空正文丢弃；图片不强制存储（存 URL）。

### DP-1 去重（P0）
- 判定规则（多级）：
  1. URL 指纹 MD5 精确去重；
  2. 标题归一化（去标点/空白、繁→简、全半角统一）后相似度 ≥ 阈值；
  3. 正文 SimHash（64 位），汉明距离 ≤ 3 视为重复。
- **保留原则（权威优先级）**：巨潮/交易所公告原文 > 官方媒体/政策原文 > 财联社/见闻（首发者优先）> 东财/同花顺转载 > 社交。
- **合并展示**：同一事件多源只保留一条主记录，其余作为"相关报道（N 家来源）"关联展示，不重复刷屏。
- 验收：同一事件多源抓取，流中仅出现 1 条主记录；误杀率 ≤ 5%。

### DP-2 自动分类（P0 基础 / P1 细化）
- **类型维度**：快讯 / 深度 / 公告 / 传闻 / 宏观 / 财报解读（公告按监管类型细分，见 FR-11）。
- **市场维度**：A股 / 港股 / 美股 / 宏观 / 商品 / 汇率（初期至少 A股 + 宏观 + 海外）。
- **行业维度（P1）**：申万一级行业为基准词典 + 概念板块（AI、算力、机器人、低空经济等动态维护）。
- **事件类型（P1）**：政策 / 业绩预告 / 并购重组 / 增减持 / 回购 / 停复牌 / 解禁 / 立案调查 / 分红 等——规则关键词 + 词典为主，模型为辅。
- 验收：类型分类准确率 ≥ 90%；行业标注命中率 ≥ 80%（抽检）。

### DP-3 情感分析（P1，三分类：正面/中性/负面）
- 快讯/深度/传闻参与分析；**公告默认中性**（公告是事实披露，市场解读才带情绪），仅对公告中明确影响方向的内容（如预增/预亏）打方向标签。
- 实现：中文金融情感词典 + 规则（否定词/程度词/主体限定）+ 模型微调（可基于 BERT 系中文金融语料）。
- 输出：`sentiment ∈ {pos,neu,neg}` + 置信度；分析主体应落到**相关个股/行业**（同一新闻对不同股票方向可能相反，需按主体分析，避免粗粒度误判）。
- 验收：人工抽检 500 条准确率 ≥ 80%；负面误报（将中性公告判为负面）≤ 5%。

### DP-4 热度与重要性计算（P0）
按产品经理定义区分两个概念：
- **热点消息（热度绝对排名高）**：展示于热点榜。
- **重要消息（热度快速上升 + 事件性质重要）**：用于高亮与推送。

**热度分草案**：
```
Heat(t) = Σ_i [ w_i(来源权威系数) × f_i(多源共振：同去重组内来源数) ]
          × 时间衰减 α^(Δt)  × (1 + 情感冲击系数)
权威系数建议：公告/官方 1.0，财联社/见闻 0.8，东财/同花顺 0.6，社交 0.15（含传闻惩罚）
时间衰减：半衰期约 30–60 分钟
```
- **上升速度**：短窗（5 分钟）Heat 增量 / 平滑基线，超过阈值 → 触发"重要消息"候选。
- **重要消息硬规则（直接判重要，免热度）**：停复牌、立案调查、退市风险、重大重组、监管政策/降准降息等（事件类型白名单）。
- **防刷/防劣质热点**：社交来源即使热度高也不得直接成为"重要消息"；营销号/黑名单源权重清零；单来源刷量需多源共振验证。
- 验收：重要消息中"传闻/营销"占比 < 5%；热点榜可解释（展示贡献来源）。

### DP-5 关联个股（P0）
- 代码识别：A股代码规则（沪 600/601/603/605/688，深 000/001/002/003/300/301，北交所 8xxxxx/4xxxxx，B股 900/200 视需要）+ 上下文校验。
- 名称识别：维护"简称 → 代码"词典（含更名、*ST 处理），与代码正则互为校验。
- **消歧**：同名简称（如"中航"系列、行业词与公司名重叠）需结合上下文（公告落款主体、共现实体、市场板块）消歧，无法确定时降级为低置信关联或不关联。
- 输出：`stock_code ↔ news` 多对多关系 + 关联置信度（高/中/低）与关联原因（代码出现/名称出现/公告主体）。
- 公告类：以公告所属上市公司主体为**确定性关联**（巨潮数据自带证券代码）。
- 验收：抽检关联准确率 ≥ 85%；知名个股（如贵州茅台、宁德时代）召回率 ≥ 90%。

### DP-6 时效指标（P0，拆环节定义）
| 环节 | 指标 | 目标 |
|---|---|---|
| 源发布 → 采集入库 | 采集延迟 | P95 ≤ 60s |
| 入库 → 处理完成（去重/分类/关联） | 处理延迟 | P95 ≤ 90s |
| 入库 → 信息流/搜索可见 | 展示延迟 | P95 ≤ 60s |
| **端到端（源发布 → 平台可见）** | **快讯时效** | **P95 ≤ 3 分钟，P99 ≤ 5 分钟** |
| 判定重要 → 企微发出 | 推送延迟 | P95 ≤ 30s |
- 验收：线上持续监控打点，达标率 ≥ 95%。

### DP-7 行情联动（异动加权与标注，P1，v1.1 新增）
- **定位**：行情仅作**增强信号**，不改变"资讯聚合"主产品定位（产品边界见 §1.4）。
- 用途① 热度/重要性加权：若关联个股当日异动（如 |涨跌幅| ≥ 5%、涨停/跌停、换手率或量比超阈值），Heat × 异动系数（建议 1.2），并作为"重要消息"候选的加分项。
- 用途② 异动事件识别：识别涨停/跌停/炸板/龙虎榜/大幅跳空等事件，作为事件类型标签（与 FR-11 事件类型体系统一管理）与个股页/信息流标注。
- 用途③ 硬规则联动：突发公告 + 该股盘中异动 → 重要等级提升（示例：突发停牌公告 + 盘中大幅拉升 → 立即判为重要并推送）。
- 数据：DR-12 延时行情快照，交易时段 15–30s 覆盖写 Redis；非交易时段冻结为当日收盘值；行情源中断自动切换"无行情模式"，资讯主链路不阻塞。
- 验收：异动标签命中率 ≥ 95%（对照当日涨跌停/龙虎榜名单）；行情中断时信息流可用性与时效不受影响。

---

## 5. 功能需求（FR）

### 5.1 功能需求明细表
| 编号 | 模块 | 需求描述 | 优先级 | 验收标准（要点） |
|---|---|---|---|---|
| FR-01 | 实时信息流 | 首页按时间倒序展示聚合资讯流，实时增量（轮询或 WebSocket），支持无限滚动 | P0 | 首屏 ≤1s；新消息 ≤30s 内出现在流中；快讯标注发布时间与来源 |
| FR-02 | 分类筛选 | 按类型（快讯/深度/公告/传闻）+ 市场（A股/宏观/海外）+ 行业 Tab 筛选，支持组合筛选与多选排除 | P0 | 筛选响应 P95 ≤300ms；Tab 数量 5 秒内可理解 |
| FR-03 | 全文搜索 | 标题+正文全文检索；支持股票代码/名称/简称联想；结果高亮、按时间/相关度排序；范围=近 7 日数据 | P0 | 联想 ≤200ms；结果 P95 ≤500ms；空结果给出建议 |
| FR-04 | 个股主页 | 输入/点击个股进入聚合页：该股公告、新闻、传闻、情感倾向汇总、来源分布（7 日内） | P0 | 页面 P95 ≤400ms；公告/新闻/传闻分区正确 |
| FR-05 | 自选股订阅 | 用户维护自选股列表（增删、排序），作为个性化推送与个股聚合的输入 | P0(内部)/P1(对外) | 增删即时生效；订阅上限（如 50 只）可配置 |
| FR-06 | 重要消息甄别 | 依据 DP-4 判定"重要"，信息流置顶/高亮展示，并进入推送候选 | P0 | 判定命中后流内高亮 ≤1min；规则可后台配置 |
| FR-07 | 热点榜 | 按实时/当日/近7日 展示热度榜（区分热点与重要），展示关联标的与来源 | P1 | 榜单 ≤1min 粒度更新；可点击回跳详情 |
| FR-08 | 传闻管理 | 传闻独立频道/标识：醒目"未经证实"角标、注明来源平台与原文外链、灰显降噪；**默认不推送**；用户可标记"求证"（后续对澄清公告） | P0 | 每条传闻必有"未经证实"标识与外链；传闻不进入重要消息 |
| FR-09 | 企业微信推送 | 见 §5.2 详细设计 | P0 | 见 §5.2 验收 |
| FR-10 | 资讯详情页 | 标题/摘要/正文(如授权)/来源与原文链接/发布时间/关联个股/相关报道(去重组)/情感标签 | P0 | 详情打开 ≤500ms；外链跳转经安全校验 |
| FR-11 | 公告中心 | 公告按监管类型（业绩预告/快报、重组、增减持、回购、质押、停复牌、立案、解禁、分红等）筛选与检索；对关键公告抽取结构化要点（金额、比例、日期区间） | P1 | 类型字典可维护；结构化抽取准确率 ≥ 80% |
| FR-12 | 后台管理 | 数据源启停与监控、规则/词典/黑名单维护、推送任务日志、数据清理任务状态、分类纠错反馈闭环 | P1 | 运营可在 10 分钟内完成一次词典更新并生效 |
| FR-13 | 用户体系 | 注册/登录（推荐手机号或微信 OAuth）、偏好与订阅存储 | P1 | 认证安全合规（见 COM-6）；订阅数据可导出/删除 |
| FR-14 | 市场情绪看板（增值） | 按日聚合全市场/行业情绪指数（正面-负面占比），盘后更新 | P2 | 与人工抽样对照偏差 ≤10% |
| FR-15 | 定时摘要（增值） | 早报（隔夜外盘+盘前公告）、收盘晚报（当日要闻+公告汇总）推送 | P2 | 交易日定时送达；内容可点回明细 |
| FR-16 | 移动端适配 | 响应式 Web（移动优先）；PWA 可缓存 | P1 | 主流机型 Chrome/Safari 可用，布局无错位 |
| FR-17 | 行情联动（异动提示，v1.1 新增） | 个股主页展示当日涨跌幅/换手/成交额与异动标签（涨停/跌停/龙虎榜/大幅跳空）；信息流中涉及异动个股的消息标注"⚡异动"；支持按"异动"事件类型筛选（依赖 DR-12/DP-7） | P1 | 异动标签命中率 ≥ 95%；行情源故障时页面降级为无行情模式且不报错 |

### 5.2 企业微信推送详细设计（FR-09）
- **推送对象（Q1 已按 §12 结论锁定）**：内部团队/订阅群（群机器人 + 自建应用消息），不引入外部 C 端与邮件/公众号备选渠道；推送层保留"渠道适配器"扩展点便于后续变更。
- **触发规则**：
  - 触发源 = DP-4 判定为"重要消息"（硬规则 + 热度上升）且**非传闻**；
  - 若用户已订阅自选股，仅推送与该股强关联（置信度高）的重要消息；
  - 未登录/未订阅用户默认推全局重大消息（降准降息、重大政策等，可配置）。
- **推送内容模板**：标题 + 一句话摘要 + 类型标签 + 关联标的 + 来源与时间 + 详情链接。
- **频控与防骚扰**：同去重事件只推 1 条；同一标的 5 分钟内不重复推；单用户每日推送上限（默认 20 条可配）；免打扰时段（默认 22:00–07:00，交易时段外可切换为摘要模式）。
- **企微限制**：群机器人 Webhook 上限约 20 条/分钟，需本地排队+限流；应用消息走官方 API 与 member 白名单。
- **可观测**：推送成功/失败/点击埋点；失败重试 2 次后告警。
- 验收：重要消息判定后 P95 ≤30s 发出；重复推送率（同事件 30min 内）≈ 0；单日推送量不触发企微限流。

### 5.3 各端信息架构（示意）
- **Web 端**：顶部 = 搜索 + 自选股入口；主导航 = 快讯 / 深度 / 公告 / 传闻 / 热点榜；首页 = 信息流（左栏筛选，右栏热度榜/重要消息区）。
- **企微推送 = 触达渠道**，不是完整产品端；点击跳转 Web 详情页。

---

## 6. 合规与法律（COM）（财经专家重点）

| 编号 | 类别 | 要求 | 措施建议 |
|---|---|---|---|
| COM-1 | 内容版权 | 快讯/深度/社交正文属第三方版权 | 默认只展示"标题+摘要+来源+原文外链"，**不全文镜像**；商用前与财联社等洽谈数据授权；保留删除配合（通知-删除）机制 |
| COM-2 | 公告转载 | 公告为法定披露信息，转载需规范 | 注明"来源：巨潮资讯网/上交所/深交所"，链回原文，禁止改篡；巨潮为指定平台优先接入 |
| COM-3 | 爬虫合规 | robots.txt / ToS / 反不正当竞争风险 | 每源评估 robots 与用户协议；限速、UA 标识、退避；社交源禁止强爬（见 DR-08/09 建议走授权/采购） |
| COM-4 | 传闻风险 | 不实信息传播可能引发侵权与市场风险 | 传闻必须标识"未经证实"，注明来源平台与链接；社交源权重极低且默认不推送；提供举报入口；涉及上市公司可联动其"澄清公告"对照（P2） |
| COM-5 | 投资建议资质 | 提供"买卖建议/收益预期"需证券投资咨询资质 | 产品边界声明：仅资讯聚合，**不构成投资建议**；全站显著位置免责声明；内容不出现"推荐买入/目标价"类运营动作 |
| COM-6 | 个人信息 | 用户手机号、自选股、行为日志 | 隐私政策 + 用户协议；数据最小化；支持导出/注销删除；若对外运营需按《个人信息保护法》评估 |
| COM-7 | 平台资质（若对外公开运营） | ICP 备案、网络安全评估等属地要求 | 发布前由法务确认；私有/内部部署可豁免部分 |
| COM-8 | 国际数据 | Finnhub 等海外源 | 确认 License 是否允许再分发；存储位置与跨境传输合规（对外运营时） |
| COM-9 | 审计留痕 | 推送、删除、规则变更 | 日志留存 ≥ 180 天（与正文 7 天策略分离，见 §7） |

---

## 7. 数据保留策略（R）——"服务器仅保留 1 周"执行方案

| 编号 | 数据类别 | 在线保留 | 策略 |
|---|---|---|---|
| R-1 | 资讯正文/标题（快讯、深度、传闻） | **7 天** | 每日定时清理任务，删除到期数据并同步重建搜索索引 |
| R-2 | 公告 | 7 天（索引期）；如需延长可仅存"标题+代码+巨潮链接" | 公告原文以巨潮官网为准，平台可只链不存，降低版权与存储压力 |
| R-3 | 去重指纹/缓存 | 短窗（≤24h） | Redis 自动过期 |
| R-4 | 推送日志/操作审计 | ≥ 180 天（**与正文策略分离**） | 冷存储，低成本对象存储/日志库 |
| R-5 | 用户数据（自选股等） | 永久（用户主动删除除外） | 与新闻数据分库 |
| R-6 | 用户行为统计/热度快照 | 聚合后可长期留存（脱敏） | 仅存聚合指标，不存明细 |
- **产品提示**：搜索/榜单明确标注"近 7 日"范围，避免用户预期落空。
- 合规存证需求（法律纠纷取证）通过 R-4 与巨潮原文链接覆盖，而非延长正文保留。
- 验收：清理任务每日执行成功、清理量可观测、无查询命中已过期数据（接口层拦截）。

---

## 8. 非功能需求（NFR）（架构师视角）

| 编号 | 类别 | 需求 | 目标值（草案） |
|---|---|---|---|
| NFR-1 | 性能 | 信息流/详情/个股页接口延迟 | P95 ≤ 300ms（首屏 ≤ 1s） |
| NFR-2 | 性能 | 全文搜索 | P95 ≤ 500ms，联想 ≤ 200ms |
| NFR-3 | 吞吐 | 内容写入能力（含去重/NLP） | 支撑日入库 ≥ 3 万条、峰值 ≥ 100 条/分钟（可按量评估扩容） |
| NFR-4 | 并发 | 初期支撑同时在线 | ≥ 1,000（按 10,000 扩展设计，无状态服务横向扩展） |
| NFR-5 | 可用性 | 核心读链路可用性 | ≥ 99.5%；采集源故障自动降级不影响浏览 |
| NFR-6 | 可靠性 | 队列削峰、失败重试、幂等 | 消息不丢不重（at-least-once + 幂等去重）；RPO ≤ 5min，RTO ≤ 30min |
| NFR-7 | 可观测性 | 全链路监控 | 采集成功率/延迟、去重率、NLP 积压、推送成功率、接口延迟、清理任务状态；告警接入企微 |
| NFR-8 | 安全 | Web 安全 | HTTPS；内容展示 XSS 清洗；外链白名单+`rel="noopener nofollow"`；接口限流防刷；密钥托管（不落代码库） |
| NFR-9 | 可扩展 | 新增数据源成本 | 新增一源 ≤ 3 人日（Adapter 模式），不侵入核心 |
| NFR-10 | 兼容性 | 浏览器 | 近 2 年 Chrome/Edge/Safari；移动端优先响应式 |
| NFR-11 | 成本 | 服务器资源（与 7 天保留配合） | MVP 单机/轻量集群可承载；预留容量方案文档化 |
| NFR-12 | 行情快照时效 | 行情快照更新延迟（交易时段） | P95 ≤ 30s；非交易时段冻结当日值（DR-12） |

---

## 9. 系统架构建议（软件架构师视角）

### 9.1 总体架构（MVP 可用简化版）
```
┌─────────────┐  ┌──────────────┐  ┌──────────────┐
│  数据源(多)  │→ │  采集层 Adapter│→ │ 消息队列(MQ)  │
│ 快讯/公告/行情│  │ 调度/限速/重试 │  │ 削峰+解耦     │
└─────────────┘  └──────────────┘  └──────┬───────┘
                                          ▼
┌────────────────────────────── 处理层(Worker 集群) ─────────────────────────────┐
│ 清洗标准化 → 去重(Redis 指纹) → 分类 → 情感NLP → 关联个股 → 热度/重要性计算        │
└──────┬───────────────────────────────────────────────────────────────────────┘
       ▼
┌───────────────────────────────────────────────────────────────────────────────┐
│ 存储层：PostgreSQL(结构化) + Elasticsearch(搜索/7天 ILM) + Redis(热度/缓存/指纹)  │
│         对象存储(可选：公告PDF/审计日志)                                          │
└──────┬───────────────────────────────────────────────────────────────────────┘
       ▼
┌───────────────────────────────────────────────────────────────────────────────┐
│ 服务层：REST API + WebSocket(实时流) → 前端 Web(移动优先)                        │
│ 推送层：判定器 → 企微渠道适配器(限流/重试/模板)  → 企业微信(群机器人/应用消息)        │
│ 运维层：监控告警(Prometheus+Grafana) / 日志 / 定时清理任务 / 后台管理              │
└───────────────────────────────────────────────────────────────────────────────┘
```

### 9.2 分层职责
1. **采集层**：每源一 Adapter（含限速/UA/代理/重试/增量游标），产出标准化原始记录；行情源（DR-12）走独立行情网关 Adapter（交易时段 15–30s 快照，源中断自动降级为"无行情模式"）；
2. **消息队列**：Kafka/RabbitMQ（或托管版），保证削峰与解耦；处理失败可重放；
3. **处理层**：无状态 Worker；NLP（情感/分类/实体）独立为 Python 服务，与主服务语言解耦；MVP 可先用轻量任务队列（如 Redis Stream/Celery）；
4. **存储层**：PostgreSQL 主库；Elasticsearch 做搜索（**索引生命周期 7 天自动删除**，对应 R-1）；Redis 存短窗热度、去重布隆/指纹、缓存、实时榜单（Sorted Set 窗口）与行情快照（KV 覆盖写）；
5. **服务层**：前后端分离；实时流用 WebSocket（或 SSE 降级轮询）；全部 API 鉴权+限流；
6. **推送层**：重要消息判定器 → 渠道适配器（企微为首，预留邮件/钉钉），内置频控与重试；
7. **运维层**：指标打点（NFR-7）、每日清理任务（R-1）、源健康看板。

### 9.3 技术选型建议
| 层 | 选项 | MVP 简化方案 |
|---|---|---|
| 后端 | Go / Java(Spring) / Python(FastAPI) | Python(FastAPI)+Go 可后置；单语言优先 |
| NLP | 词典+规则起步 → 中文金融模型（如 FinBERT 类微调） | 先规则+词典，预留模型接口 |
| 前端 | Vue3 / React | Vue3 + Vite，移动优先 |
| 数据库 | PostgreSQL | 单实例起步 |
| 搜索 | Elasticsearch（或先 PostgreSQL 全文索引过渡） | PG 全文索引（数据 7 天较小，可承受） |
| 缓存/榜单 | Redis | Redis（含 Stream 作轻量队列） |
| 队列 | Kafka / RabbitMQ / Redis Stream | Redis Stream 起步，接口抽象后可替换 |
| 推送 | 企微 Webhook/应用消息 | 企微群机器人 Webhook + 限流队列 |
| 部署 | Docker Compose → K8s（量级上来后） | Docker Compose + 定时任务 |

### 9.4 核心数据模型（草案）
```text
news            (id, dedup_group_id, source_id, source_url, title, summary,
                 content?, type, market, industries[], event_types[],
                 sentiment, sentiment_conf, heat, importance, is_hot,
                 published_at, crawled_at, status)
news_stock      (news_id, stock_code, stock_name, relation_type, confidence)   -- 多对多
source          (id, name, category, enabled, freq, last_success_at, config)
stock_dict      (code, name, short_names[], market, industry, listed, updated_at) -- 简称词典
user            (id, phone/openid, settings)
watchlist       (user_id, stock_code, sort_no, created_at)
push_log        (id, news_id, channel, target, result, sent_at, clicked_at)
dedup_fingerprint(Redis key: 标题指纹/SimHash, TTL 24h)
quote_snapshot  (Redis KV: stock_code → {price, change_pct, turnover_rate, amount, ts})  -- 交易时段 15–30s 覆盖写（DR-12）
```
> 关键索引：`news(published_at desc)`, `news_stock(stock_code, news_id)`, `news(type, published_at)`, 搜索走 ES/PG 全文。

### 9.5 API 概览（草案）
| 方法 | 路径 | 说明 |
|---|---|---|
| GET | /api/v1/feed | 信息流（type/market/industry/page/cursor） |
| GET | /api/v1/news/{id} | 详情 + 相关报道 |
| GET | /api/v1/search?q= | 全文搜索 |
| GET | /api/v1/stocks/{code}/news | 个股聚合 |
| GET | /api/v1/stocks/{code}/quote | 个股行情快照（延时，交易时段） |
| GET | /api/v1/quotes?codes= | 批量行情快照（信息流异动标注/列表联动，DR-12） |
| GET/POST/DELETE | /api/v1/watchlist | 自选股 |
| GET | /api/v1/hot | 热点榜 |
| POST | /api/v1/notify/wecom/webhook | 推送回调/配置 |
| GET/POST | /api/v1/admin/* | 源管理/词典/规则/清理/日志 |
| WS | /ws/v1/feed | 实时增量流 |

---

## 10. 里程碑规划（产品经理视角）

| 阶段 | 周期 | 交付内容 | 退出标准（Demo 可演示） |
|---|---|---|---|
| M0 合规与调研 | 1 周 | 数据源版权/接入方式确认；接口 Demo 验证（财联社/东财/巨潮 各拉通 1 条真实数据）；技术选型冻结 | 法务与数据源授权结论 OK；POC 跑通端到端 1 条快讯 |
| M1 MVP | 4 周 | DR-01/03/05 接入；DP-1 去重、DP-4(简化)、DP-5 关联、DP-6 打点；FR-01/02/03/04/06/08/09/10；后台最小集（源开关+推送日志）；数据清理任务(R-1) | 快讯端到端 P95 ≤3min；去重与关联抽检达标；重要消息可推企微并点击回跳 |
| M2 增强 | 3–4 周 | DR-02/06/07、DR-12(行情快照)；DP-2 细化(行业/事件)、DP-3 情感、DP-7 行情联动；FR-05/07/11/13/16/17；后台完整化（词典/黑名单/规则）；监控告警完备 | 分类/情感/公告结构化抽检达标；自选股订阅推送闭环；异动联动标注可用 |
| M3 增值/扩展 | 可选 | DR-08~11 合规接入；FR-14/15；移动端优化；用户运营与 KPI 看板 | KPI 可量化观测；按需评估商业化 |

> 建议并行：MVP 期间同步推进 M2 中"简称词典 + 事件类型词典"的积累（词典是关联与分类质量的生命线）。

---

## 11. 主要风险清单

| 风险 | 等级 | 影响 | 缓解措施 |
|---|---|---|---|
| 数据源被封/接口变更（财联社等） | 高 | 快讯缺失，产品失效 | Adapter 隔离+监控告警；多源互备；与源方建立授权合作；首发权优先级兜底 |
| 版权投诉（内容镜像） | 高 | 法律风险 | 摘要+外链模式；删除响应机制；商用前授权；正文不落地持久化（7 天策略） |
| 传闻被当事实传播 | 高 | 用户误导/侵权 | 传闻强标识、默认不推送、低权重；澄清公告联动（P2）；免责声明 |
| 重要消息误判/漏判 | 中 | 推送骚扰或错过关键 | 硬规则白名单 + 热度上升双重判定；人工抽检反馈闭环；推送频控 |
| NLP 效果（中文金融实体/情感） | 中 | 质量不达标 | 词典优先策略；置信度门槛；抽检看板；预留模型替换接口 |
| 社交源强爬被诉 | 中 | 法律/封号 | 走授权 API 或第三方舆情采购；元数据为主（见 COM-3） |
| 行情源限额/断连/版权（免费源尤甚） | 中 | 异动联动失效 | 延时行情多源互备 + "无行情模式"自动降级；免费源限频；必要时采购 Level-1 授权 |
| 服务器/API 成本超支 | 中 | 成本风险 | 7 天保留策略；冷热分离；源频率上限配置；成本监控 |
| 企微限流/推送失败 | 低 | 触达失效 | 排队限流 + 重试 + 告警；渠道适配器预留备选 |
| 对外运营合规未明（资质/备案） | 中 | 发布延期 | M0 由法务明确范围；先私有/内部部署上线验证 |

---

## 12. 待确认问题与评审结论

> **业务方评审结论（v1.1）**：Q4 行情联动 → **确认引入延时行情联动**；其余问题不再逐项决策，按下表"默认执行结论"锁定。若后续"其它不需要"指向删除某项需求本身（如 FR-13 用户体系、FR-15 定时摘要），请指明条目，将同步降级或删除。

| 编号 | 原问题 | 默认执行结论（已锁定） |
|---|---|---|
| Q1 | 企微推送对象与形态？是否增加备选渠道？ | 内部团队/订阅群（群机器人+自建应用）；备选渠道不引入，仅保留渠道适配器扩展点 |
| Q2 | 是否需要注册登录与自选股云同步？ | MVP 不做 C 端用户体系：自选股订阅以管理端配置/白名单先行；FR-13 保留为对外运营时的 P1 选项 |
| Q3 | 财联社/雪球等数据版权采购预算？ | 维持"标题+摘要+外链、不镜像全文"免采购策略；接到版权主张或商用前再立项评估 |
| Q4 | 是否引入行情联动？ | ✅ **引入**：延时行情快照用于异动联动（DR-12/DP-7/FR-17），不含 Level-2/分笔与交易决策，列入 M2 |
| Q5 | 搜索历史范围？ | 近 7 日（与 R-1 一致）；公告更长历史以"链回巨潮原文"方式解决 |
| Q6 | 对外公开运营 or 私有部署？ | 私有/内部部署先行；对外运营（ICP/安全评估/个人信息）触发时再评审 COM-7/COM-8 |
| Q7 | 技术栈偏好？ | 无既有约束：按 §9.3 建议选型，M0 结束前冻结 |
| Q8 | 初期规模档位？ | 按 NFR-4"1,000 在线"档设计容量，预留 10,000 档扩展路径 |
| Q9 | 早报/晚报摘要优先级？ | 维持 P2（FR-15），M2 通过推送点击率数据验证价值后再评估 |

---

## 附录 A：术语表

| 术语 | 含义 |
|---|---|
| 快讯 | 以"电报"形式的高时效短消息（多为事件首发） |
| 深度 | 有分析内容的报道/解读文章 |
| 公告 | 上市公司依法定渠道（巨潮/交易所）披露的文件 |
| 传闻（小作文） | 未经官方证实、来自社交平台的市场传言 |
| 重要消息 | 事件性质重要或热度快速上升、需主动触达用户的消息 |
| 热点消息 | 绝对热度排名高的消息（上榜展示，不一定推送） |
| 去重组 | 同一事件多源报道聚合后的主记录及其关联报道集合 |
| 多源共振 | 同一事件被多个独立来源报道的现象（可信度与热度加权依据） |
| SimHash | 文本相似度指纹算法，用于内容级去重 |
| Adapter | 数据源接入适配器（可插拔的采集插件） |
| ILM | Elasticsearch 索引生命周期管理（配合 7 天清理） |

---

*本文件为需求规格说明书 v1.1（草案基线）。标注"草案/建议"的数值与范围需在评审会上确认后固化；v1.1 已确认事项（行情联动等）见 §12 评审结论。*
